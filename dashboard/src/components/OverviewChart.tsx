type OverviewChartProps = {
  view: "ventas" | "viajes";
  isDashboard: boolean;
};

// Continue here: min: 04:44:53
const OverviewChart = ({ isDashboard = false, view }: OverviewChartProps) => {
  return <div>OverviewChart</div>;
};

export default OverviewChart;
